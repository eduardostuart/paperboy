name: Deliver newspaper
on:
  schedule:
    # Run every day at 6AM
    - cron: "0 6 * * *"
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run
        run: cargo run
